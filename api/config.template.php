<?php
/**
 * Конфигурация для уведомлений через PHP
 * Скопируйте этот файл в config.php и заполните своими данными
 *
 * ВАЖНО: Добавьте api/config.php в .gitignore чтобы не выложить секретные данные!
 */

$config = [
    // Email конфигурация
    'email' => [
        // Включить отправку email
        'enabled' => false, // Установите true после настройки

        // Адрес получателя (куда будут приходить уведомления)
        'to_email' => 'your-email@yandex.ru',

        // Адрес отправителя (от кого будет письмо)
        'from_email' => 'noreply@yourdomain.ru',

        // SMTP настройки (для Яндекс, Gmail и других)
        // Если оставить пустым, будет использоваться стандартная функция mail()
        'smtp_host' => 'smtp.yandex.ru', // SMTP сервер
        'smtp_port' => 465, // Порт: 465 (SSL) или 587 (TLS)
        'smtp_user' => 'your-email@yandex.ru', // Логин SMTP (обычно ваш email)
        'smtp_pass' => 'your-app-password', // Пароль приложения

        // Для Яндекса:
        // 1. Перейдите на https://id.yandex.ru/security
        // 2. Включите двухфакторную аутентификацию
        // 3. Создайте пароль приложения для SMTP
        //
        // Для Gmail:
        // 1. Перейдите в настройки аккаунта Google
        // 2. Безопасность → Двухэтапная аутентификация
        // 3. Пароли приложений → Создать пароль для приложения
    ],

    // Шаблон письма
    // Доступные переменные: {{from_name}}, {{from_email}}, {{phone}}, {{artwork}}, {{message}}
    'emailTemplate' => 'Новый запрос с сайта Вячеслава Пешкина

Контактная информация:
Имя: {{from_name}}
Email: {{from_email}}
Телефон: {{phone}}

Интересующая картина:
{{artwork}}

Сообщение:
{{message}}

---
С уважением,
Форма обратной связи сайта',

    // Telegram конфигурация
    'telegram' => [
        // Включить отправку в Telegram
        'enabled' => false, // Установите true после настройки

        // Токен бота (получить через @BotFather)
        'bot_token' => 'YOUR_BOT_TOKEN',

        // ID чата куда отправлять сообщения
        // Узнать можно через @userinfobot или через API:
        // https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
        'chat_id' => 'YOUR_CHAT_ID',

        // Инструкция по созданию бота:
        // 1. Напишите @BotFather в Telegram
        // 2. Отправьте команду /newbot
        // 3. Следуйте инструкциям для создания бота
        // 4. Сохраните полученный токен
        // 5. Напишите что-нибудь вашему боту
        // 6. Откройте https://api.telegram.org/bot<TOKEN>/getUpdates
        // 7. Найдите "chat":{"id":123456789} - это ваш chat_id
    ]
];

// Не изменяйте эту строку
return $config;
